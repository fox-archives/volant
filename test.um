
main: func() int = func() int {
    str, num: *char, int = "hehehehe", 100+ 20 + 100+(10+10);

    mem: *char = malloc(sizeof(char)*10);

    printf("Exter something: ");
    scanf("%s", mem);
    printf("You entered %s.\nRandom stuff below...\n", mem);

    free(mem);

    if x: int = 0; num {
        printf("str is \"%s\" and num is %i.\n", str, num);
    } else if x {
        printf("hehe x is not xero.\n");
    } else {
        printf("hehe x is zero.\n");
    }
    
    for i: int = 10; i; --i {
        printf("i is %i and num is %i\n", i, num);
    }

    switch p: int = 0; num {
    case 90:
        printf("num is 90.\n");
        break;
    default:
        printf("num is not 90.\n");
    }

    return 0;
};
